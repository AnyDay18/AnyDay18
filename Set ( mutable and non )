# Define a set and determine if it's mutable or immutable with an example example_set = {1, 2, 3} 
# Sets are mutable - you can add or remove elements from a set example_set.add(4) 
print("Example set:", example_set) 
# Create a set with values 1, 2, 3, 4, 4, 5 and print its type my_set = {1, 2, 3, 4, 4, 5} 
print("Type of my_set:", type(my_set)) 
# Update the set with new string items: "apple", "banana" my_set.update(["apple", "banana"]) print("Updated set:", my_set) 
# Perform set union, intersection, and difference operations other_set = {3, 4, 5, 6, 7} print("Union:",my_set.union(other_set)) print("Intersection:",my_set.intersection(other_set)) 
print("Difference:",my_set.difference(other_set)) 
